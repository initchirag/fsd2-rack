# Redux Toolkit + Reselect Demo

This project is a simple React app that demonstrates:

- centralized state with Redux Toolkit
- normalized post data
- derived state with memoized selectors
- async loading through a mock thunk

## Run

```bash
npm install
npm run dev
```

## Verify

```bash
npm run lint
npm run build
```
